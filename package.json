{
  "name": "wheelraffle",
  "private": true,
  "version": "0.1.0",
  "description": "Local wheel-spin raffle app: React frontend + Node/Express backend.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run start --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
